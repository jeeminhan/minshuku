# QA Report — contract 005, round 01

| # | Criterion | Verdict | Evidence |
|---|-----------|---------|----------|
| C1 | Seed command works and produces seeded shape | PASS | `npm run seed-demo` exit 0; all jq pins verified: `.day==4`, `.pending==null`, `.recentContext=={"lastTemplateId":"bookshop-quiet-browse","lastLocation":"bookshop"}`, 3-line summary starting "Day 1:"/"Day 2:"/"Day 3:", `[.reviewItems[].itemId]` exact 7-item order |
| C2 | Seed is deterministic (byte-identical re-run) | PASS | `cp` run1 → `/tmp/seed-run1.json`; re-ran seed; `cmp` exit 0 |
| C3 | SRS values are engine-computed, not hand-typed | PASS | All 7 items' SRS pins exact (tsumori 4/2.65/06-05, mado 1/2.5/06-02, ame 4/2.65/06-06, fushigi 1/2.5/06-04, three fresh items all null); `grep -c "2026-06-0" seed-demo.ts` = 0; `grep -c "applyOutcome" seed-demo.ts` = 0; both `runEpisode` and `completeEpisode` present |
| C4 | Seeded day's episode cast from evolved state | PASS | `curl .../api/episode`: `.status=="completed"` `.story.day==4` ✓; summary byte-equal to state file ✓; all 3 summary lines verbatim in promptContext ✓; `.log.templateId=="minshuku-arrival-with-mom"` ✓; activeTargetsChosen `["grammar.temo-ii","vocab.yakusoku"]` ✓; items length 4, active set `{grammar.temo-ii,vocab.yakusoku}`, passive set `{vocab.mado,vocab.motsu}` ✓; `grammar.temo-ii` surface `てもいい` reading null ✓; `vocab.motsu` surface `持つ` reading `もつ` meaning `to hold; to have` ✓; each player turn has exactly 2 evaluatorResults with itemIds `{grammar.temo-ii,vocab.yakusoku}` ✓; NPC contains `持つ` ×1 and `窓` ×1, contains `てもいい` ×0 and `約束` ×0 ✓; repeat GET byte-identical under exclusion list ✓; reviewItems unchanged by GETs ✓ |
| C5 | Completing seeded day: debrief pins, tsumori returns, day 5 fails loud, re-seed resets | PASS | POST `/api/episode/complete` HTTP 200; `.day==5` ✓; 4-line summary, first 3 byte-identical to seeded, 4th "Day 4: Mom welcomed..." ✓; learned sort `["vocab.mado","vocab.motsu"]` ✓; strengthened `[{grammar.temo-ii,produced},{vocab.yakusoku,mastered}]` ✓; dueTomorrow `["vocab.motsu","vocab.mado","vocab.fushigi","grammar.tsumori","grammar.temo-ii"]` ✓; state pins yakusoku/temo-ii exact ✓; day-5 GET = HTTP 500 with non-empty `.error` mentioning fixture/day ✓; re-seed `cmp` byte-identical to seed-run1.json ✓ |
| C6 | Seeded day in browser: day indicator + story-so-far (1440×900) | PASS | Playwright 1440×900; exactly 1 GET /api/episode ✓; `/Day 4/` matches ✓; `[data-testid="story-so-far"]` visible before interaction ✓; contains Day 1:, Day 2:, Day 3: ✓; day-3 line verbatim ✓; `[data-role="coach"]` visible ✓; `[data-role="npc"][data-turn="2"]` visible ✓; no `[data-turn]` > 2 in DOM ✓. Screenshot: `/tmp/c6-1440-opening.png` |
| C7 | Seeded-day playthrough + debrief (1440×900) | PASS | 3 player turns submitted; exactly 2 `[data-token-item]` elements (vocab.motsu, vocab.mado) ✓; 6 `[data-outcome]` badges in player turns ✓; exactly 1 POST 200 ✓; all 3 `[data-debrief-group]` visible ✓; learned 2 items with 持つ and 窓 ✓; strengthened 2 items — temo-ii has `てもいい` + `data-outcome="produced"` ✓, yakusoku has `約束` + `data-outcome="mastered"` ✓; due-tomorrow 5 items with exact set {vocab.motsu, vocab.mado, vocab.fushigi, grammar.tsumori, grammar.temo-ii} ✓; tsumori entry contains `つもり` ✓; return-tomorrow visible matching `/Day 5/` ✓; GET count stayed 1 ✓; zero pageerror events ✓; zero console errors ✓; zero responses ≥400 ✓. Screenshots: `/tmp/c7-1440-debrief.png` |
| C8 | Reset-to-day-1 untouched by bigger demo deck | PASS | `rm -f` state file; `curl .../api/episode` jq check `day==1 && summary=="" && items.length==5 && active sort==["grammar.tsumori","vocab.mado"] && templateId=="cafe-regular-encounter"` ✓; second GET byte-identical ✓; state file `reviewItems.length==5` exact 5-item order ✓; all `nextReviewAt` null ✓; grammar.temo-ii and vocab.motsu absent from reviewItems ✓ |
| C9 | Story-so-far absent on fresh day 1 (browser) | PASS | Playwright 1440×900 on fresh state; `/Day 1/` in page ✓; `[data-testid="story-so-far"]` count = 0 (not rendered) ✓; coach beat and NPC turn 2 visible ✓; no data-turn > 2 ✓; zero console errors ✓. Screenshot: `/tmp/c9-1440-day1.png` |
| C10 | Seeded day responsive at 375×812 | PASS | Re-seeded, Playwright 375×812; `[data-testid="story-so-far"]` visible ✓; no horizontal overflow on load (`scrollWidth ≤ innerWidth+1`) ✓; story-so-far right edge = 355 ≤ 376 ✓; after playthrough: 3 debrief groups visible ✓; return-tomorrow visible ✓; no horizontal overflow with debrief ✓; all three `[data-debrief-group]` right edge = 355 ≤ 376 ✓; zero console errors ✓. Screenshots: `/tmp/c10-375-opening.png`, `/tmp/c10-375-debrief.png` |
| C11 | README `## Demo` commands work verbatim | PASS | `## Demo` section present with 2 fenced `sh` blocks; `grep seed-demo` matches inside section ✓; section states re-running resets to day 4 ✓. Fresh flow: `rm -f web/.data/story-state.json` exit 0; `MINSHUKU_FAKE_LLM=1 npm --prefix web run dev` backgrounded on port 3000; `curl .../api/episode \| jq '.story.day==1'` exit 0 ✓. Seeded flow: `npm --prefix web run seed-demo` exit 0; existing server serves day 4; `jq '.story.day==4'` exit 0 ✓ |
| C12 | Gates + hygiene | PASS | `npm run code-check`: 22 test files, 101 tests passed ✓; `cd web && npm run lint` exit 0 ✓; `npm run build` exit 0 (4 routes) ✓; `git status --porcelain -- src/lib` empty ✓; `episode-demo-learner-day3.json` and `episode-demo-learner-day4.json` present ✓; grep counts ≥1 for template names in respective fixtures ✓; `applyOutcome` and `pickDueItems` in web appear only in `storyStore.ts` importing from engine ✓; `newReviewItem(` count = 6 (1 declaration + 5 seed calls, file untouched) ✓ |

## Verdict: PASS (all 12 criteria)

## Failures — each must be actionable without further investigation

None.

## Out-of-contract findings (not graded)

- The `episode-demo-learner-day2.json` fixture filename differs from the naming pattern used by day3/day4 fixtures (which include the word "learner"). This is a cosmetic inconsistency, not a functional issue.
- The `demoLearner.ts` file shows `M` git status (carried over from contract 004, per generator-state.md note 7) — not part of this contract's scope but worth noting for the next commit.

## Console errors observed

None across all tested viewports (1440×900 on seeded day, 1440×900 on day 1, 375×812 on seeded day).
