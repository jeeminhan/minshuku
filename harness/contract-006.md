# Contract 006 — Polish + responsive pass (stage one: mechanical floor)
Backlog item: "民宿 aesthetic applied end to end (existing art/audio assets), 375/768/1440 clean, loading/error/empty states, no console errors."

Scope: Stage one only — the mechanically-verifiable floor. A code + live audit (fixture mode, port 3020, 2026-06-12) found the app already in good shape: 768px is overflow-free through the whole journey, gloss toggle + aria-pressed work, focus-visible outlines exist, lang="ja" coverage is complete, the day-5 error UI renders role="alert" correctly, and the complete-POST pending state works. Confirmed gaps to fix, with evidence:

1. **Loading state is invisible to tooling and AT** — the "Preparing today's episode…" `<p>` in `web/components/episode/EpisodePlayer.tsx` (loading phase, ~line 115) has no `role="status"`, no `aria-live`, and no `data-testid` (audit B1: `{"role":null,"testid":null,"ariaLive":null}`). → add `data-testid="episode-loading"` + `role="status"`.
2. **Loading pulse ignores reduced motion** — computed `animation-name` of the loading indicator is `pulse` even though `.turn-enter` correctly resolves to `none` under `prefers-reduced-motion: reduce` (audit B1 vs C1). Tailwind's bare `animate-pulse` does not respect the media query. → `motion-safe:` variant or CSS override in `web/app/globals.css`.
3. **create-next-app scaffold leftovers** — `web/public/` still holds unused `next.svg`, `vercel.svg`, `globe.svg`, `window.svg`, `file.svg` (zero references in `web/app`/`web/components`), and `web/app/favicon.ico` is the stock Next favicon (md5 `c30c7d42707a47a3f4591831641e50dc`). → delete the SVGs, replace the favicon with a minshuku-specific one.
4. **Untested-but-coded paths get regression pins** — error state in-browser (day-5 500), complete-pending state, gloss close-on-second-tap, 768px viewport, 500-char player input, tab order. These pass today (audit A3–A8, E1–E3); the criteria below pin them so the stage-two taste pass cannot silently break them.

Files expected to change: `web/components/episode/EpisodePlayer.tsx` (loading state attributes), `web/app/globals.css` (reduced-motion override if needed), `web/app/favicon.ico`, `web/public/*` (deletions only).

Out of scope: stage two (human taste pass — visual judgment, aesthetic quality, art direction; no judgment-based criteria here); wiring `public/audio/*.m4a` ambient audio (feature work, not floor); `public/art/` backdrops (directory contains only markdown style-bible docs, no image assets exist — nothing to wire); rewriting the day-5 fail-loud error message (its dev-facing wording is intentional per contract 005); the `skipped` API phase (not reachable in fixture mode); engine code (`src/lib` read-only); deploy (contract 007).

## Criteria (each must be mechanically checkable by a browser-driving evaluator)

All browser checks run against a production build on port 3020 with `MINSHUKU_FAKE_LLM=1` (never live Gemini). Reset state (`rm -f web/.data/story-state.json`) before fresh-day-1 checks; `npm --prefix web run seed-demo` before seeded checks. "Standing exclusions" = `jq 'del(.log.id, .log.startedAt, .log.endedAt, .log.llmLatencyMs)'`. "No horizontal overflow" = `document.documentElement.scrollWidth <= window.innerWidth + 1`.

- [ ] C1 — **Loading state is announced and testable.** Playwright 1440×900, fresh day 1, with `page.route` delaying `GET /api/episode` by ~2000ms: while pending, an element `[data-testid="episode-loading"]` is visible, has `role="status"`, contains non-empty text, and the brand mark (`民宿 MINSHUKU`) is visible above it. After the delay resolves, the normal opening beat renders (`[data-role="coach"]` visible) and exactly 1 GET `/api/episode` was made.
- [ ] C2 — **Loading animation respects reduced motion.** Playwright context with `reducedMotion: 'reduce'`, same delayed-GET setup: computed `animation-name` of `[data-testid="episode-loading"]` is `none`. Regression: in the same context after load, computed `animation-name` of the first `.turn-enter` element is also `none`. In a default (no-preference) context, the loading element's computed `animation-name` is NOT `none` (the pulse still exists for everyone else).
- [ ] C3 — **Day-5 error state renders cleanly in the browser at 768×1024.** Setup via curl: seed-demo, `GET /api/episode` (creates pending), `POST /api/episode/complete` (advances to day 5). Then Playwright 768×1024 loads `/`: `[role="alert"]` is visible, its text contains both `Could not load today’s episode.` and the substring `fixture` (the server's `.error` passed through); `[data-testid="player-input"]` count is 0; no horizontal overflow; zero `pageerror` events; zero console errors EXCEPT messages matching `/Failed to load resource/` (the browser logs the 500 fetch itself — exactly one such message is allowed). Re-seed after this check.
- [ ] C4 — **Complete pending state is honest and double-click-proof.** Playwright 1440×900, fresh day 1, full playthrough; `page.route` delays `POST /api/episode/complete` by ~1500ms. After clicking `[data-testid="complete-episode"]`: while pending, the button's text is `Closing the day…` and `disabled === true`; a second forced click during the pending window produces no additional request (total POST count stays exactly 1); after resolution, `[data-testid="complete-confirmation"]` and all 3 `[data-debrief-group]` elements are visible.
- [ ] C5 — **768×1024 full journey is clean (previously untested viewport).** Fresh day 1 at 768×1024: no horizontal overflow on load, after every player turn is submitted, and after completing to the debrief; `section[aria-label="Today’s dialogue"]` width ≤ 960px; `[data-testid="player-input"]` and `[data-testid="player-submit"]` visible on initial load; `[data-testid="return-tomorrow"]` visible at the end; zero console errors and zero `pageerror` events across the journey.
- [ ] C6 — **500-character player input does not break layout.** At 375×812 AND 768×1024, fresh day 1: fill `[data-testid="player-input"]` with `'あ'.repeat(500)`, submit. The typed text appears inside the revealed `[data-role="player"]` turn; no horizontal overflow; the player turn card's bounding-box right edge ≤ viewport width + 1.
- [ ] C7 — **Gloss token is a true toggle (regression pin).** Fresh day 1, 1440×900: click the first `[data-token-item]` — a tray entry appears under the NPC line containing the item's surface, reading, and meaning, and the token's `aria-pressed` is `"true"`. Click the same token again — the tray entry is removed from the DOM (count returns to its pre-click value) and `aria-pressed` is `"false"`. The NPC sentence's visible text content is byte-identical before, during, and after.
- [ ] C8 — **Keyboard floor: tab order and focus-visible.** Fresh day 1, 1440×900: tabbing from the document start reaches, in DOM order, the gloss token(s) inside NPC turn 2 before `[data-testid="player-input"]`, then `[data-testid="player-submit"]` (sequence check over the focusable cycle). Computed styles: `[data-testid="player-input"]` when focused has `outline-style: solid` with `outline-width` ≥ 2px; a `[data-token-item]` button and `[data-testid="player-submit"]` under `:focus-visible` (use `el.focus({focusVisible:true})` and assert `el.matches(':focus-visible')`) each have `outline-style: solid` with `outline-width` ≥ 2px.
- [ ] C9 — **All Japanese text carries `lang="ja"` (regression pin).** Fresh day 1, 1440×900: a TreeWalker over `document.body` text nodes matching `/[぀-ヿ一-龯]/` finds zero nodes whose parent element lacks a `[lang="ja"]` ancestor — checked at three moments: opening beat, after full playthrough (including an open gloss tray and outcome badges), and on the debrief view (all three `[data-debrief-group]`s rendered).
- [ ] C10 — **Scaffold purge + non-stock favicon.** `ls web/public` contains none of `next.svg`, `vercel.svg`, `globe.svg`, `window.svg`, `file.svg`; `md5 -q web/app/favicon.ico` ≠ `c30c7d42707a47a3f4591831641e50dc` (the create-next-app stock favicon) and the file is non-empty; `curl -s -o /dev/null -w '%{http_code}' http://localhost:3020/favicon.ico` returns 200; `grep -rn "next.svg\|vercel.svg\|globe.svg\|window.svg\|file.svg" web/app web/components` returns nothing.
- [ ] C11 — **Determinism + happy-path regression guard.** Fresh day 1: two consecutive curl GETs of `/api/episode` are byte-identical under standing exclusions. Playwright 1440×900 full journey: `[data-testid="story-so-far"]` count is 0 on day 1; coach beats, `[data-outcome]` badges (6 total), all 3 `[data-debrief-group]`s and `[data-testid="return-tomorrow"]` render as in contracts 003–005; exactly 1 GET and 1 POST; zero console errors, zero `pageerror` events, zero responses ≥ 400. Then seed-demo: `[data-testid="story-so-far"]` visible on day 4 with Day 1/2/3 lines (contract 005 C6 shape intact).
- [ ] C12 — **Gates + hygiene.** `npm run code-check` exits 0 (101 tests); `cd web && npm run lint` exits 0; `npm run build` exits 0; `git status --porcelain -- src/lib` is empty (engine untouched).

## Evaluator review

**ACCEPTED.** All twelve criteria are mechanically verifiable with zero judgment calls. C1's loading-state trigger is explicit (route interception delaying GET /api/episode ~2000ms). C2's reduced-motion emulation method is named (Playwright `reducedMotion: 'reduce'`, with positive control). C10's md5 check is a not-equal guard that survives stage-two favicon replacement; liveness + non-empty checks prevent deletion-without-replacement. No aesthetic/taste criteria leaked in (stage two is human). Console allowance precisely specified in C3 (exactly one `/Failed to load resource/`). The `skipped` API phase exclusion is appropriate (unreachable in fixture mode).

*(Appended by orchestrator verbatim from evaluator output — the review agent returned its verdict without writing it.)*
